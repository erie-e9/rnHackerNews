import { Touchable } from '@components/atoms';
import styled from 'styled-components/native';

export const FeaturesContainer = styled.View`
  flex-direction: row;
  width: 60px;
  right: 0px;
`;

export const FeatureButton = styled(Touchable)``;
